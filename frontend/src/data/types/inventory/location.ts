export interface Location {
    id: number;
    title: string;
    address: string;
    description: string;
}

export interface LocationApiDTO {
    id: number;
    title: string;
    address: string;
    description: string;
}
